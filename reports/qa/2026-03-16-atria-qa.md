═══════════════════════════════════════════════════
ATRIA DAILY QA REPORT
Date: 2026-03-16 21:00 MST
Build: f29c83c
Changes detected: NO (last commit 2026-03-14 17:46:55 -0600)
Modules run: Smoke Tests, Console Audit, Health Metrics
═══════════════════════════════════════════════════

OVERALL STATUS: CRITICAL

⚠ CRITICAL ISSUES REQUIRE IMMEDIATE ATTENTION — see blocking issues below

───────────────────────────────────────────────────
SUMMARY

No code changes in the last 48 hours. Smoke tests only. The persistent
Next.js build cache corruption issue (first flagged 2026-03-15) remains
unresolved. Both `next dev` and `next build` fail with ENOENT errors for
manifest files (`routes-manifest.json`, `pages-manifest.json`). Root cause
is likely Node.js v25.6.1 incompatibility with Next.js 15.5.12. The
dashboard renders on initial load but API routes and sub-pages return 500.
This is the single most important issue to fix — it blocks all development
and testing.

───────────────────────────────────────────────────
RESULTS BY MODULE

  Smoke Tests                  FAIL
  Changed Feature Tests        SKIPPED (no changes in 48 hours)
  Console Audit                FAIL
  Health Metrics               WARNING (degraded — partial data only)

───────────────────────────────────────────────────
BLOCKING ISSUES (CRITICAL — fix before next deployment)

  1. Next.js build fails — routes-manifest.json never generated
     Where: .next/routes-manifest.json (and .next/server/pages-manifest.json during prod build)
     User experience: API routes return 500, property detail pages crash
       with black screen, dashboard stats show "--" until second reload
     Likely cause: Node.js v25.6.1 incompatibility with Next.js 15.5.12.
       Node 25 is bleeding-edge current release. The filesystem operations
       Next.js uses to write manifest files appear to fail silently.
     Suggested fix: Pin Node.js to v20 LTS or v22 LTS via .nvmrc or
       engines field in package.json. Alternatively, upgrade Next.js to
       a version that explicitly supports Node 25.

  2. Vendor chunks not generated for @supabase
     Where: .next/server/vendor-chunks/ (only @swc.js and next.js present)
     User experience: Property detail page crashes with MODULE_NOT_FOUND
       error for ./vendor-chunks/@supabase.js — black screen
     Likely cause: Same Node.js 25 build issue — webpack vendor chunk
       splitting fails silently.

───────────────────────────────────────────────────
HIGH PRIORITY (address within 24 hours)

  1. Webpack cache pack file rename failures
     Where: .next/cache/webpack/client-development-fallback/0.pack.gz
     User experience: No direct user impact but causes cascading build
       failures and stale chunk references after HMR updates
     Likely cause: File rename operation (atomic write pattern) failing
       on Node 25 fs API

───────────────────────────────────────────────────
MEDIUM AND LOW FINDINGS

  MEDIUM:
  - Dashboard stat cards show "--" during initial load while API calls
    are in flight (~1-2s). Not a bug but a UX consideration.
  - All test properties contain SQL injection attempt names
    ("Robert); DROP TABLE pr...") — rendering is safe (no XSS), but
    the test data is noisy. Consider cleaning test database.
  - 4 ESLint warnings for <img> usage instead of next/image in:
    inspection-flow.tsx, property-detail.tsx (2x), media-upload.tsx

  LOW:
  - webpack.cache.PackFileCacheStrategy serialization warning for
    big strings (133kiB) — performance advisory only

───────────────────────────────────────────────────
SMOKE TEST RESULTS

  App launch              PASS (Ready in ~1000ms)
  Auth flow               PASS (user logged in as eric.j.unterberger@gmail.com)
  Property loads          PARTIAL (API returns 200 with 50 properties on 2nd
                          attempt; first attempt often 500 due to manifest issue)
  Baseline retrieval      SKIPPED (property detail page crashes before
                          baseline can be tested)
  Camera permission       SKIPPED (web app, not testable in browser)
  Vision API reachable    SKIPPED (cannot reach property detail / inspection
                          flow due to build crash)
  Navigation              FAIL (dashboard renders but property detail and
                          all sub-routes crash with MODULE_NOT_FOUND)

SMOKE STATUS: FAIL
CRITICAL FAILURES: Build system broken — manifest files not generated

───────────────────────────────────────────────────
CONSOLE AUDIT

  CRITICAL entries: 50+ (all routes-manifest.json ENOENT errors)
  HIGH entries: 2 (MODULE_NOT_FOUND for vendor-chunks/@supabase.js,
                    MODULE_NOT_FOUND for ./611.js, ./996.js)
  MEDIUM entries: 1 (webpack cache serialization warning)

  CRITICAL:
    ENTRY: [Error: ENOENT: no such file or directory, open '.../.next/routes-manifest.json']
    SOURCE: Next.js internal build pipeline
    LIKELY CAUSE: Node.js v25.6.1 fs API incompatibility

  CRITICAL:
    ENTRY: Cannot find module './vendor-chunks/@supabase.js'
    SOURCE: .next/server/app/property/[id]/page.js
    LIKELY CAUSE: Vendor chunk not generated during incomplete build

  HIGH:
    ENTRY: Cannot find module './611.js' / './996.js'
    SOURCE: .next/server/pages/_error.js and .next/server/app/api/properties/route.js
    LIKELY CAUSE: Webpack chunk IDs reference files that were never written

───────────────────────────────────────────────────
HEALTH METRICS

  Full inspection flow (camera to results): SKIPPED (build broken)
  API latency (Vision API call only): SKIPPED
  Image upload time: SKIPPED

  App startup time (cold launch to usable): ~1000ms (Ready message)
  Auth flow completion time: <500ms (redirect 307 → login 200)

  Console errors at startup: 0 (initial compile succeeds)
  Console errors during first API call: 10+ (manifest ENOENT cascade)

  Supabase storage accessible: YES (property cover image loaded from
    zcjfeafmblqfjiglvmzz.supabase.co — 200 OK)

  API response data (when working):
    /api/properties: 200 OK, 50 properties returned
    /api/inspections: 200 OK, 50 inspections returned
    Trained properties: 1 of 50

───────────────────────────────────────────────────
HEALTH METRICS TREND

  App startup time:    1000ms (today) vs ~1000ms (2026-03-15) — stable
  API latency:         N/A (today) vs N/A (2026-03-15) — both blocked
  Build stability:     FAIL (today) vs FAIL (2026-03-15) — no improvement
  Startup errors:      0 → 10+ after first navigation (same as 2026-03-15)

  REGRESSION: None new. Same critical build issue persists from 2026-03-15.
  No code changes were made, so no new regressions are expected.

───────────────────────────────────────────────────
ENVIRONMENT

  Node.js: v25.6.1 (current/unstable — NOT LTS)
  Next.js: 15.5.12
  OS: Darwin 25.3.0 (macOS)
  Package manager: npm
  Supabase: accessible (storage + auth confirmed working)

───────────────────────────────────────────────────
RECOMMENDATION

The build system has been broken for 2+ days. No code changes can be
properly tested until this is resolved. Recommended immediate action:

  1. Install Node.js 20 LTS: `nvm install 20 && nvm use 20`
  2. Add .nvmrc: `echo "20" > .nvmrc`
  3. Clean install: `rm -rf .next node_modules && npm install`
  4. Verify: `npm run dev` and confirm all routes serve without 500s

This should be the highest priority fix before any feature work resumes.

═══════════════════════════════════════════════════
END OF REPORT — ATRIA DAILY QA
═══════════════════════════════════════════════════

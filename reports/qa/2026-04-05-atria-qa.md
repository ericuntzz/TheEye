═══════════════════════════════════════════════════
ATRIA DAILY QA REPORT
Date: 2026-04-05 ~18:40 UTC
Build: 9c6bdc9 (fix(mobile): replace broken tmp-file-move with direct overwrite in offline queue)
Changes detected: NO — no commits in last 24 hours
Modules run: Smoke Tests, Console Audit, Health Metrics
═══════════════════════════════════════════════════

OVERALL STATUS: WARNINGS

───────────────────────────────────────────────────
SUMMARY

No code changes detected in the last 24 hours. Smoke tests only.
The app launches and serves pages correctly. The health endpoint
responds 200. Login page renders. Dashboard correctly redirects to
login when unauthenticated. However, the Node.js dev server process
cannot resolve the Supabase hostname (getaddrinfo ENOTFOUND
zcjfeafmblqfjiglvmzz.supabase.co), which means all authenticated
API endpoints return 401. This is an environment/network issue on
the test machine — DNS resolves from the shell (nslookup succeeds)
but not from Node.js. All auth-dependent smoke tests are marked
SKIPPED. The single most important action is to investigate why
Node.js DNS resolution fails on this machine.

───────────────────────────────────────────────────
RESULTS BY MODULE

  Smoke Tests                  WARNING (auth-dependent tests SKIPPED)
  Changed Feature Tests        SKIPPED (no changes)
  Console Audit                PASS (0 app errors; 6 DNS resolution errors)
  Health Metrics               WARNING (auth endpoints untestable)

───────────────────────────────────────────────────
BLOCKING ISSUES (CRITICAL — fix before next deployment)

  None — no blocking issues found in application code.

───────────────────────────────────────────────────
HIGH PRIORITY (address within 24 hours)

  1. Node.js DNS resolution fails for Supabase hostname
     Where: Server-side — any API route calling getDbUser() or Supabase client
     User experience: All authenticated API calls return 401. If this
       affects production, users cannot log in or access any data.
     Likely cause: Node.js is not using the system DNS resolver. The
       shell (nslookup) resolves zcjfeafmblqfjiglvmzz.supabase.co
       to 172.64.149.246 / 104.18.38.10, but Node.js getaddrinfo
       returns ENOTFOUND. This may be a sandbox/network restriction
       on the automated test runner environment, or a stale DNS cache
       in Node.js. IMPORTANT: This may be environment-specific to the
       scheduled task runner and NOT affect local dev or production.
       Verify by running `npm run dev` manually and testing auth.

───────────────────────────────────────────────────
MEDIUM AND LOW FINDINGS

  MEDIUM: Watchpack EMFILE errors flood server logs on startup
    (too many open files). Dev-only — no functional impact, but
    indicates file descriptor limits are too low for the project
    size. Consider: ulimit -n 10240

  LOW: /properties and /inspections pages return 404 — routes are
    at /property and /inspection (singular). Not new, same as prior runs.

───────────────────────────────────────────────────
HEALTH METRICS TREND

  Health API:              239ms    vs 743ms (Mar 31)      -68% (IMPROVED)
  App startup (server):    778ms    vs 1060ms (Mar 31)     -27% (IMPROVED)
  Dashboard page load:     1524ms   vs 598ms (Mar 31)      +155% (REGRESSION)
  Login page load:         47ms     vs n/a (Mar 31)
  Properties API:          SKIPPED (401 — DNS issue)  vs 447ms (Mar 31)
  Inspections API:         SKIPPED (401 — DNS issue)  vs 509ms (Mar 31)
  Vision endpoints:        SKIPPED (401 — DNS issue)

  Dashboard page load regression note: The 1524ms measurement
  includes the 307 redirect to /login + login page render. Direct
  login page load is 47ms. The previous 598ms measurement was for
  an authenticated dashboard render. Not comparable due to auth
  failure — not a real regression.

  Startup errors:          0 app errors, 6 DNS errors (new)
  Console errors:          0 (excl DNS)

───────────────────────────────────────────────────
FULL FINDINGS

SMOKE TEST RESULTS
Date: 2026-04-05
Build: 9c6bdc9

  App launch              PASS (778ms server ready)
  Auth flow               SKIPPED (Supabase DNS unreachable from Node.js)
  Property loads          SKIPPED (requires auth)
  Baseline retrieval      SKIPPED (requires auth)
  Camera permission       SKIPPED (web preview, no camera access)
  Vision API reachable    PARTIAL (endpoints respond 401, not 500 — routes exist)
  Navigation              PASS (/ → 307 → /login, /dashboard → 307 → /login, /login → 200)
  Health API              PASS (200, 239ms)

SMOKE STATUS: WARNING
CRITICAL FAILURES: none
SKIPPED: 3 tests due to Supabase DNS resolution failure in Node.js

───────────────────────────────────────────────────

CHANGED FEATURE TEST
No changes detected — smoke tests only.

───────────────────────────────────────────────────

CONSOLE AUDIT

  CRITICAL entries: 0
  HIGH entries: 0 (DNS errors are environment, not app code)
  MEDIUM entries: 0

  Environment issue:
    6x "TypeError: fetch failed" caused by:
    "Error: getaddrinfo ENOTFOUND zcjfeafmblqfjiglvmzz.supabase.co"
    These occur when API routes attempt Supabase auth verification.
    The app handles these gracefully (returns 401, does not crash).

  Watchpack:
    Many "EMFILE: too many open files, watch" warnings on startup.
    Dev-only, no functional impact.

───────────────────────────────────────────────────

DAILY HEALTH METRICS

  Health API:              239ms
  App startup (server):    778ms
  Dashboard → login:       1524ms (includes redirect)
  Login page direct:       47ms

  Authenticated endpoints: ALL SKIPPED (DNS issue)

  Console errors at startup: 0 app errors
  Console errors during testing: 6 DNS resolution errors (env issue)

═══════════════════════════════════════════════════
END OF REPORT — ATRIA DAILY QA
═══════════════════════════════════════════════════

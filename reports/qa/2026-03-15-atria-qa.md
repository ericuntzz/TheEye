═══════════════════════════════════════════════════
ATRIA DAILY QA REPORT
Date: 2026-03-15 ~22:00 MST
Build: f29c83c (Improve inspection reliability: lower thresholds, auto-start, quality gate)
Changes detected: NO (last commit ~28 hours ago)
Modules run: Smoke Tests, Console Audit, Health Metrics
═══════════════════════════════════════════════════

OVERALL STATUS: CRITICAL

!! CRITICAL ISSUES REQUIRE IMMEDIATE ATTENTION -- see blocking issues below

---

SUMMARY

No code changes in the last 24 hours (last commits were March 14 at 17:46 MST). Smoke tests
were run against the current build. The app launches and the login page renders correctly. Auth
works (existing session redirects to dashboard). Properties API, Inspections API, and Baselines
API all return 200 with correct data. However, the Next.js dev server suffers from repeated
.next build cache corruption -- webpack-compiled chunks and manifest files disappear during
runtime, causing 500 Internal Server Errors after a few navigations. This happened on every
server instance tested (3 restarts). The single most important thing to fix is the .next cache
instability.

---

RESULTS BY MODULE

  Smoke Tests                  WARNING (5/7 pass, 1 partial, 1 skipped)
  Changed Feature Tests        SKIPPED (no changes in 24h window)
  Console Audit                FAIL (repeated ENOENT errors in server logs)
  Health Metrics               WARNING (cache corruption prevents sustained testing)

---

BLOCKING ISSUES (CRITICAL -- fix before next deployment)

  1. .next build cache corruption causes 500 errors during navigation
     Where: .next/server/ directory -- routes-manifest.json, app-paths-manifest.json,
             pages-manifest.json, and webpack chunks (e.g. ./996.js) disappear
     User experience: "Internal Server Error" page after 3-5 navigations/API calls.
                      App becomes completely unusable until server restart.
     Likely cause: Next.js 15.5.12 webpack cache race condition. The
                   webpack.cache.PackFileCacheStrategy also reports ENOENT on
                   rename operations for .pack.gz files, suggesting filesystem
                   contention or a known Next.js caching bug. Consider:
                   - Upgrading Next.js to latest patch
                   - Setting `webpack.cache: false` in next.config
                   - Checking for antivirus/filesystem watcher interference
     Reproduction: Start dev server with clean .next, load dashboard, make 3+ API
                   calls or navigate to different pages. Server starts returning 500s.

---

HIGH PRIORITY (address within 24 hours)

  None beyond the critical issue above.

---

MEDIUM AND LOW FINDINGS

  MEDIUM: Next.js 15.5.12 is flagged as "outdated" by the Next.js dev overlay
  MEDIUM: webpack.cache.PackFileCacheStrategy warns about serializing big strings (133kiB)
  LOW: 50 properties include SQL injection test entries ("Robert); DROP TABLE properties;--")
       -- these are safely escaped and rendered as text, confirming XSS/SQLi protection works.
       Consider cleaning test data for production readiness.

---

HEALTH METRICS

  App startup time (cold launch to ready):  ~950ms (consistent across 3 restarts)
  Dashboard compilation:                    ~450ms (1156 modules)
  Properties API response:                  722ms (50 properties)
  Inspections API response:                 ~1100ms first call, ~370ms cached
  Baselines API response:                   1367ms (first compile + query)
  Vision API endpoint reachability:         834ms (returns 400 for test payload -- correct)
  Time to .next corruption:                 ~3-5 API calls / navigations

  Startup errors at launch:                 0
  Errors after navigation:                  10+ (all ENOENT on manifest files)

  No previous run data available for trend comparison.

---

SMOKE TEST RESULTS

  App launch              PASS    (starts in <1s, compiles correctly)
  Auth flow               PASS    (existing session redirects to dashboard; login page renders)
  Property loads          PASS    (50 properties load, detail page renders with rooms/baselines)
  Baseline retrieval      PASS    (Eu V1 trained property has 1 baseline version, API returns 200)
  Camera permission       SKIPPED (requires mobile device context)
  Vision API reachable    PASS    (POST /api/vision/compare-stream returns 400 for test payload)
  Navigation              WARNING (works initially, breaks after .next cache corruption)

  SMOKE STATUS: WARNING
  CRITICAL FAILURES: .next cache corruption (see blocking issue #1)

---

CONSOLE AUDIT

  CRITICAL entries: 10+ (all ENOENT errors on .next manifest/chunk files)
  HIGH entries: 0
  MEDIUM entries: 1 (webpack big string serialization warning)

  LEVEL: CRITICAL
  ENTRY: Error: ENOENT: no such file or directory, open '.next/routes-manifest.json'
  SOURCE: Next.js server internals (webpack-runtime.js, require.js)
  LIKELY CAUSE: Webpack cache files being deleted or overwritten during compilation

  LEVEL: CRITICAL
  ENTRY: Error: Cannot find module './996.js' (Require stack: webpack-runtime.js)
  SOURCE: .next/server/app/dashboard/page.js:2:8093
  LIKELY CAUSE: Compiled webpack chunk missing from .next/server/ after cache corruption

  LEVEL: MEDIUM
  ENTRY: webpack.cache.PackFileCacheStrategy - Serializing big strings (133kiB)
  SOURCE: webpack cache layer
  LIKELY CAUSE: Large module serialization in cache; performance warning only

---

CHANGED FEATURE TEST

  SKIPPED -- No code changes detected in the 24-hour window.
  Last commits (March 14):
    f29c83c - Improve inspection reliability: lower thresholds, auto-start, quality gate
    6a78c51 - Fix: wrap middleware updateSession in try/catch to prevent bare 500s

  Note: These commits are close to the 24h boundary (~28 hours old). The middleware
  fix (6a78c51) is working -- the middleware compiles and runs without errors. The
  inspection reliability changes (f29c83c) could not be fully end-to-end tested due
  to the .next cache corruption preventing sustained navigation.

═══════════════════════════════════════════════════
END OF REPORT -- ATRIA DAILY QA
═══════════════════════════════════════════════════
